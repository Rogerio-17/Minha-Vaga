<div id="vagas-view-container" class="container">
  <div class="row text-center">
    <a class="back-link main-color" href="/">Voltar</a>
    <div id="img-container" class="col-md-12">
      <img src="/img/companhia.png" alt="codigo" />
    </div>

    <div class="col-md-12">
      <h1 class="main-color">{{vaga.dataValues.title}}</h1>
      <p class="bold">Qual profissional estamos buscando: </p>
      <p class="job-desc">{{vaga.dataValues.description}}</p>
      <p><span class="bold">O salário informando é de: </span>
        R${{vaga.dataValues.salary}}</p>
      <p>
        Para tabalhar na
        <span class="bold main-color">{{vaga.dataValues.company}}</span>
        basta enviar um email para
        <a href="mail:to{{vaga.dataValues.email}}">{{vaga.dataValues.email}}</a>
      </p>
    </div>
  </div>
</div>