<!-- FORM -->
<div id="add-form-container" class="container">
  <div class="row">
    <div class="col-md-12">
      <h1 class="text-center">
        Divulgue sua vaga preenchendo o formaulario abaixo
      </h1>
    </div>

    <div id="form-box" class="col-md-12">
      <h2>
        Preencha os dados da melhor forma possível para encontra o dev que se
        encaixa melhor na vaga.
      </h2>
      <form action="/vagas/add" method="POST">
        <!-- // -->
        <div class="form-group">
          <label for="title">Título da vaga: </label>
          <input
            type="text"
            class="form-control"
            id="title"
            name="title"
            placeholder="Digite o título da vaga"
            required
          />
          <small id="titleHelp" class="form-text text-muted">O título da vaga é
            bastante importante, tente passar de forma mais clara e objetiva
            possível.</small>
        </div>

        <!-- // -->
        <div class="form-group">
          <label for="description">Descrição da vaga: </label>
          <textarea
            type="text"
            class="form-control"
            id="description"
            name="description"
            placeholder="Digite a descrição da vaga"
            required
          ></textarea>
        </div>

        <!-- // -->
        <div class="form-group">
          <label for="company">Nome da empresa: </label>
          <input
            type="text"
            class="form-control"
            id="company"
            name="company"
            placeholder="Digite o nome da empresa"
            required
          />
          <small id="titleHelp" class="form-text text-muted">Nome referente a
            empresa que está contratando.</small>
        </div>

        <!-- // -->
        <div class="form-group">
          <label for="email">Email para contato: </label>
          <input
            type="text"
            class="form-control"
            id="email"
            name="email"
            placeholder="Digite um email para contato"
            required
          />
        </div>

        <!-- // -->
        <div class="form-group">
          <label for="salary">Salário da vaga: </label>
          <input
            type="text"
            class="form-control"
            id="salary"
            name="salary"
            placeholder="Digite o salário da vaga"
            required
          />
        </div>
        <input type="hidden" name="new_job" value="1" />
        <button type="submit" class="btn btn-primary">
          Enviar informações
        </button>
      </form>
    </div>
  </div>
</div>